# 🔧 Настройка Telegram Webhook для EmotiCare

## Способ 1: Через терминал (рекомендуется)

### Шаг 1: Получите ваш домен Railway

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект `AIT`
3. Перейдите в **Settings** → **Networking**
4. Найдите **Public Domain** или нажмите **Generate Domain**
5. Скопируйте домен (например: `ait-production.up.railway.app`)

### Шаг 2: Получите токен бота

У вас должен быть токен от BotFather (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 3: Выполните команду

Откройте терминал (на Mac: Terminal, на Windows: PowerShell или CMD) и выполните:

```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ваш-домен.railway.app/api/telegram/webhook"}'
```

**Замените:**
- `ВАШ_ТОКЕН` на ваш токен от BotFather
- `ваш-домен.railway.app` на ваш домен из Railway

**Пример:**
```bash
curl -X POST "https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ait-production.up.railway.app/api/telegram/webhook"}'
```

### Шаг 4: Проверьте результат

Вы должны увидеть ответ:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

## Способ 2: Через браузер (самый простой)

### Вариант A: Через URL в браузере

1. Замените значения в этой ссылке:
```
https://api.telegram.org/botВАШ_ТОКЕН/setWebhook?url=https://ваш-домен.railway.app/api/telegram/webhook
```

2. Вставьте в адресную строку браузера и нажмите Enter

**Пример:**
```
https://api.telegram.org/bot123456789:ABCdefGHIjklMNOpqrsTUVwxyz/setWebhook?url=https://ait-production.up.railway.app/api/telegram/webhook
```

### Вариант B: Через онлайн curl сервис

1. Откройте [https://reqbin.com/curl](https://reqbin.com/curl)
2. Выберите метод **POST**
3. URL: `https://api.telegram.org/botВАШ_ТОКЕН/setWebhook`
4. В Headers добавьте: `Content-Type: application/json`
5. В Body выберите **JSON** и вставьте:
```json
{
  "url": "https://ваш-домен.railway.app/api/telegram/webhook"
}
```
6. Нажмите **Send**

## Способ 3: Через скрипт (если установлен Node.js)

1. Откройте терминал в папке проекта
2. Установите переменные окружения:
```bash
export TELEGRAM_BOT_TOKEN=ваш_токен
export WEBHOOK_URL=https://ваш-домен.railway.app
```
3. Запустите скрипт:
```bash
npm run setup:telegram
```

## Проверка webhook

После настройки проверьте, что webhook установлен:

### Через браузер:
```
https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo
```

### Через curl:
```bash
curl "https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo"
```

Вы должны увидеть информацию о webhook, включая ваш URL.

## Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить приветственным сообщением от EmotiCare

## Проблемы?

**Ошибка "Unauthorized":**
- Проверьте, что токен правильный
- Убедитесь, что токен начинается с `bot` в URL (но не в токене!)

**Ошибка "Bad Request":**
- Проверьте, что URL правильный и доступен
- Убедитесь, что домен не содержит лишних пробелов
- Проверьте, что деплой на Railway завершился успешно

**Бот не отвечает:**
- Проверьте логи в Railway Dashboard
- Убедитесь, что `TELEGRAM_BOT_TOKEN` установлен в Railway
- Проверьте, что webhook установлен правильно через `getWebhookInfo`
