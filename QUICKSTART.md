# 🚀 Быстрый старт

## 1. Создание Telegram бота (2 минуты)

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя и username для бота
4. Сохраните токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Получение Chat ID:**
- Найдите [@userinfobot](https://t.me/userinfobot)
- Начните диалог - получите ваш Chat ID

## 2. Push в GitHub

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

## 3. Деплой на Railway (5 минут)

1. Откройте [railway.app](https://railway.app)
2. Войдите через GitHub
3. **New Project** → **Deploy from GitHub repo**
4. Выберите репозиторий `ai-therapist`
5. Railway начнет автоматический деплой

## 4. Настройка переменных окружения

В Railway Dashboard → Settings → Variables:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
```

## 5. Настройка домена и webhook

1. В Railway → Settings → Networking → **Generate Domain**
2. Дождитесь завершения деплоя
3. Получите URL (например: `ai-therapist-production.up.railway.app`)

**Настройка webhook через Telegram API:**

```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ВАШ_ДОМЕН.railway.app/api/telegram/webhook"}'
```

Или используйте скрипт (после установки зависимостей локально):

```bash
export TELEGRAM_BOT_TOKEN=ваш_токен
export WEBHOOK_URL=https://ваш-домен.railway.app
npm run setup:telegram
```

## 6. Проверка работы

1. ✅ Откройте: `https://ваш-домен.railway.app`
2. ✅ Проверьте health: `https://ваш-домен.railway.app/api/health`
3. ✅ Напишите боту в Telegram: `/start`

## ✅ Готово!

Теперь вы можете:
- Использовать веб-приложение
- Управлять через Telegram бота (`/status`, `/health`, `/help`)
- Автоматический деплой при каждом `git push`

## 📱 Команды Telegram бота

- `/start` - Приветствие и список команд
- `/status` - Статус приложения (uptime, память)
- `/health` - Проверка здоровья сервиса
- `/users` - Статистика пользователей
- `/help` - Справка

## 🆘 Проблемы?

**Бот не отвечает:**
- Проверьте webhook: `curl "https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo"`
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены

**Приложение не запускается:**
- Проверьте логи в Railway → Deployments
- Убедитесь, что `npm run build` проходит успешно локально

**404 на webhook:**
- Убедитесь, что URL правильный: `https://домен/api/telegram/webhook`
- Проверьте, что деплой завершился успешно
