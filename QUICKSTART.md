# 🚀 Быстрый старт EmotiCare

## 1. Создание Telegram бота (2 минуты)

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя (например: "EmotiCare Therapist")
4. Придумайте username (например: "emoticare_bot")
5. Сохраните токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 2. Получение OpenAI API ключа (опционально, но рекомендуется)

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Перейдите в раздел **API Keys**
3. Нажмите **Create new secret key**
4. Сохраните ключ (выглядит как `sk-...`)

> **Важно:** Без OpenAI бот будет работать, но с упрощенной логикой. С OpenAI ответы намного лучше и более терапевтические.

## 3. Деплой на Railway

1. Убедитесь, что код запушен в GitHub
2. Откройте [railway.app](https://railway.app)
3. Войдите через GitHub
4. Нажмите **New Project**
5. Выберите **Deploy from GitHub repo**
6. Выберите репозиторий `AIT`

Railway начнет автоматический деплой.

## 4. Настройка переменных окружения

В Railway Dashboard → Settings → Variables:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_openai_api_key  # опционально, но рекомендуется
```

## 5. Настройка домена

1. Railway Dashboard → Settings → Networking
2. Нажмите **Generate Domain**
3. Сохраните URL (например: `ait-production.up.railway.app`)

## 6. Настройка webhook

После завершения деплоя настройте webhook:

```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://ваш-домен.railway.app/api/telegram/webhook"}'
```

Или используйте скрипт:
```bash
export TELEGRAM_BOT_TOKEN=ваш_токен
export WEBHOOK_URL=https://ваш-домен.railway.app
npm run setup:telegram
```

## 7. Проверка работы

1. ✅ Откройте Telegram
2. ✅ Найдите вашего бота по username
3. ✅ Отправьте `/start`
4. ✅ Начните диалог!

## ✅ Готово!

EmotiCare готов помочь! Просто напишите боту, и он начнет терапевтический диалог.

## 📱 Как пользоваться

1. **Начните диалог**: `/start`
2. **Поделитесь чувствами**: Просто напишите о том, что у вас на душе
3. **Отвечайте на вопросы**: EmotiCare задаст открытые вопросы
4. **Практикуйте**: Будет предлагать микро-практики (дыхание, записи)
5. **Подведение итогов**: Каждые 5-7 сообщений — summary

## 🆘 Проблемы?

**Бот не отвечает:**
- Проверьте webhook: `curl "https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo"`
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены

**Некачественные ответы:**
- Проверьте, что `OPENAI_API_KEY` установлен
- Проверьте баланс на OpenAI аккаунте
- Используйте модель `gpt-4o-mini` или `gpt-4`

**Приложение не запускается:**
- Проверьте логи в Railway → Deployments
- Убедитесь, что все переменные окружения установлены
